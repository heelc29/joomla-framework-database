name: CI Joomla Framework

on:
    push:
    pull_request:
    schedule:
        - cron: 15 2 * * 1

concurrency:
    group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
    cancel-in-progress: true

jobs:
    tests-unit-sqlsrv:
        name: Run Unit tests on SQLsrv
        runs-on: ubuntu-latest
        container: joomlaprojects/docker-images:php${{ matrix.php_version }}
        strategy:
            matrix:
                php_version: ['8.3', '8.4', '8.5']
                db_version: ['2017', '2025']
        steps:
            - uses: actions/checkout@v6
            - name: Prepare SQLSRV environment
              run: |
                  curl -sSL -O https://packages.microsoft.com/config/debian/$(grep VERSION_ID /etc/os-release | cut -d '"' -f 2 | cut -d '.' -f 1)/packages-microsoft-prod.deb
                  dpkg -i packages-microsoft-prod.deb
                  rm packages-microsoft-prod.deb
                  apt-get update
                  ACCEPT_EULA=Y apt-get install -y msodbcsql18 unixodbc-dev
                  pecl install sqlsrv && docker-php-ext-enable sqlsrv
                  php --ri sqlsrv
            - name: Run Unit tests
              run: |
                  git config --global --add safe.directory $GITHUB_WORKSPACE
                  composer update
                  ./vendor/bin/phpunit --configuration phpunit.sqlsrv.xml.dist --display-all-issues
        services:
            sqlsrv:
                image: mcr.microsoft.com/mssql/server:${{ matrix.db_version }}-latest
                env:
                    ACCEPT_EULA: Y
                    MSSQL_SA_PASSWORD: Password12!
                    MSSQL_PID: Developer
                options: >-
                    --health-cmd="bash -c 'SQLCMD=$(command -v /opt/mssql-tools/bin/sqlcmd || command -v /opt/mssql-tools18/bin/sqlcmd); [ -z \"$SQLCMD\" ] && exit 1; $SQLCMD -S localhost -U sa -P Password12! -C -Q SELECT\ 1'"
